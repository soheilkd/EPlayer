<UserControl x:Class="EPlayer.Controls.MainControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EPlayer.Controls" 
			 mc:Ignorable="d"  Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<local:MediaButtons x:Name="Controls" Margin="5"/>
		<local:VolumeSlider x:Name="VolumeSlider" Grid.Column="2" Width="100" Margin="5"/>
		<local:MediaProgressBar x:Name="ProgressBar" Grid.Column="1" VerticalAlignment="Center" />
	</Grid>
</UserControl>
